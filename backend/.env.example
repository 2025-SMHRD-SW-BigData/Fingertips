# Example environment variables (do not commit real secrets)

# Server
# Server listens on APP_PORT (PORT is optional fallback)
APP_PORT=3000
# PORT=3000

# Database
DB_HOST=project-db-campus.smhrd.com
DB_PORT=3307
DB_USER=campus_25SW_BD_p3_1
DB_PASSWORD=your_password_here
DB_NAME=fingertips
